
module JuliaSet where

import Codec.Picture
import Data.Complex

import Cli
import Color
import Glue

julia
  :: [ArgPair]
  -> (Int, Int)
  -> (Int -> Int -> PixelRGB8)
julia args dims
    = undefined
  where
    x = 0
